from logging.config import listen
from unittest import skip


listen =[]

leggtil = input("Hva skal du legge til listen?")
tavekk = input('Hva vil du ta vekk?')
for liste in listen:
    if leggtil == None:
        skip
    elif 
